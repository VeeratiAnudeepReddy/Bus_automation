# Calendar System

## Purpose
The operations calendar combines trips, schedules, maintenance, leave, and incidents.

## API
`GET /api/calendar`

Optional query:
- `from`
- `to`

## Screen
`/calendar`

## Future Work
Drag-and-drop rescheduling is pending. Current implementation provides a unified operational timeline.
