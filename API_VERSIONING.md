# API Versioning

Legacy routes remain under `/api`.

Sprint 8 adds aliases under `/api/v1`.

Legacy `/api` responses include deprecation headers. Current version metadata is available at:
- `/api/meta`
- `/api/v1/meta`
