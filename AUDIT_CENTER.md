# Audit Center

Audit center uses the existing `AuditLog` collection and `/audit` route, with Sprint 5 additions for posts and support actions.

Current support:
- timeline list
- actor population
- CSV-compatible export
- expanded target types for posts and support tickets

Pending:
- visual before/after diff viewer
- browser/device/location capture from request metadata
